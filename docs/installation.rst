============
Installation
============

At the command line::

    $ easy_install configurator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv configurator
    $ pip install configurator
